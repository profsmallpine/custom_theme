<%= form_for(@theme, remote: true) do |f| %>
  <% if @theme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theme.errors.count, "error") %> prohibited this theme from being saved:</h2>

      <ul>
      <% @theme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tenant_id %><br>
    <%= f.text_field :tenant_id %>
  </div>
  <div class="field">
    <%= f.label :font_color %><br>
    <%= f.text_field :font_color %>
  </div>
  <div class="actions">
    <br />
    <%= f.submit %>
    <%= f.submit "Preview" %>
  </div>
<% end %>

<div id="test-div">
</div>

<script>
  $('input[id=theme_font_color]').minicolors()
</script>
